services:
  docs:
    image: credit_clear_local_docs
    container_name: credit_clear_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./credit_clear:/app/credit_clear:z
    ports:
      - '9000:9000'
    command: /start-docs
